scalar Upload
scalar DateTime
scalar Void
input Filter {
  value: String
  operator: String
  field: String
}

input Order {
  orderBy: String
  order: String
}

input Pagination {
  limit: Int
  offset: Int
}

input QueryParameters {
  order: Order
  pagination: Pagination
  filters: [Filter]
}
