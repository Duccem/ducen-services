{
  "name": "@helsa/modules",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/*/**.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "build:back": "tsc --project tsconfig.back.json",
    "build:watch:back": "tsc -w --project tsconfig.back.json",
    "build:watch:front": "tsc -w --project tsconfig.json",
    "lint": "eslint \"{src,test}/**/*.ts\" --fix",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@ducen/eslint-config": "^0.0.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/jest": "^29.5.12",
    "@typescript-eslint/eslint-plugin": "^7.2.0",
    "@typescript-eslint/parser": "^7.2.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "@ducen/ports-server": "^0.0.0",
    "@ducen/ports-client": "^0.0.0",
    "@ducen/core": "^0.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  }
}
